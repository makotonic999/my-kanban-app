# dev-log: 2026-09-24（フェーズ4 完了）— 本番 SRE 設定反映（AMP + AMG）

## この日やったこと
フェーズ4 最後のタスク「本番環境への SRE 設定反映」を Terraform でコード化。
**Amazon Managed Prometheus (AMP) + Amazon Managed Grafana (AMG)** を採用し、
フェーズ2の PromQL/SLO 資産を流用。`terraform plan` 通過まで（`apply` は課金のためフロント後）。

設計判断は [`docs/adr/0001-managed-observability-amp-amg.md`](../adr/0001-managed-observability-amp-amg.md) に ADR として記録。

## 実装
- `terraform/amp.tf`: `aws_prometheus_workspace` + `aws_prometheus_rule_group_namespace`。
  remote_write URL を SSM に保存。
- `terraform/amp_rules.yml`: `slo_rules.yml` + `alert_rules.yml` を **AMP が要求する単一 `groups:` 形式**に結合
  （2 グループ / 21 ルール、YAML パース検証済み）。
- `terraform/adot.tf`: ADOT Collector 設定。`prometheus` receiver で `localhost:8080/metrics` をスクレイプ →
  `prometheusremotewrite`(sigv4auth, service=aps) で AMP へ。設定は SSM(Advanced) 経由で sidecar に注入。
- `terraform/ecs.tf`: タスク定義に **adot-collector サイドカー**（`aws-otel-collector`）を追加 + 専用ロググループ。
- `terraform/grafana.tf`: `aws_grafana_workspace`（AWS_SSO / SERVICE_MANAGED / data_sources=PROMETHEUS）+
  AMP クエリ用ロール。`enable_grafana` でトグル。
- `terraform/iam.tf`: タスクロールに `aps:RemoteWrite`、実行ロールに ADOT config の SSM 読み取りを追加。

## 検証エビデンス
- `terraform fmt` = 0、`terraform validate` = Success。
- `terraform plan` = **48 to add, 0 to change, 0 to destroy**（従来 39 + AMP/ADOT/AMG で +9）。警告・エラーなし。
  アカウントガードは沈黙（dev 123456789012 に正しく向いている）。
- `amp_rules.yml` を YAML パースし `groups=2 / rules=21` を確認。
- `go build ./...` = 0、`go.mod` クリーン。
- **`apply` 未実施**（課金なし）。

## アーキテクチャ
```
ECS task: [api :8080 /metrics] + [adot-collector sidecar]
                                        │ remote_write (sigv4)
                                        ▼
                                   AMP workspace ── rule group(SLO recording + alerts)
                                        │
                                        ▼
                        Amazon Managed Grafana (AWS SSO) ── data source: AMP
```

## 学び / 割り切り
- **既存の PromQL 資産を活かす**のが AMP 採用の決め手。CloudWatch 主軸だと SLO ルールの翻訳が必要で資産が死ぬ。
- **ステートレス+水平スケール**なアプリと、中央集約の AMP は相性が良い（複数タスクのメトリクスを 1 か所に）。
- ルール資産が二重管理（ローカル用 / AMP 結合版）になったのは割り切り。将来は単一ソースから生成したい。
- アラート通知先（SNS 等）接続、KMS CMK 化、AMG ユーザー割り当ては今後の課題。

## フェーズ4 の到達点
- [x] インフラ構成（ECS Fargate + RDS + ALB）Terraform 化・plan 通過
- [x] IaC（Terraform）
- [x] CI/CD（GitHub Actions、CI 稼働 / CD は OIDC でコード化）
- [x] 本番 SRE 設定反映（AMP + AMG + ADOT）コード化・plan 通過
- [ ] 実 `apply`（フロント完成後）

## 次のステップ（フェーズ5）
- React/Next.js でフロント、JWT ログインフロー、S3 + CloudFront ホスティング。
- フロント完成後に一連のインフラを `apply` → CD 実走 → デモ → `destroy`。
