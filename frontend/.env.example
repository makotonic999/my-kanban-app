# フロントエンドのビルド時環境変数（Vite）。
# このファイルをコピーして .env.local を作るか、CI では GitHub Variables から注入する。

# 本番 API のオリジン。未設定時は Vite 既定の "/api"（dev プロキシ経由）を使う。
# 例: https://api.example.com  または ALB の DNS 名
VITE_API_BASE_URL=
